

<div class="container mt-4">
    <h1> {{title}} </h1>

    {{# each groups}}
        <div><h3> <a  href="list-groups/{{this.id}}"> {{this.group_name}}</a></h3></div>
    {{/each}}

</div>
